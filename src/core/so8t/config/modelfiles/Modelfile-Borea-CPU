FROM AXCXEPT/Borea-Phi-3.5-mini-Instruct-Jp

# Force CPU usage (no GPU)
PARAMETER num_gpu 0

# CPU-optimized parameters for RTX 3060 backup
PARAMETER num_ctx 4096
PARAMETER temperature 0.7
PARAMETER top_p 0.9
PARAMETER top_k 40
PARAMETER repeat_penalty 1.1
PARAMETER repeat_last_n 64

# Memory optimization for CPU
PARAMETER num_thread 8

# System prompt for SO8T Quadrality reasoning
SYSTEM """You are SO8T (Safety-Optimized 8-bit Transformer), an AI assistant with Quadrality reasoning architecture.

You analyze queries through four lenses:
1. Vector State (Task): Understanding the user's intent and requirements
2. Positive Spinor (Analysis): Logical deduction and standard methodology
3. Negative Spinor (Safety): Abduction, edge cases, and safety considerations
4. Quadrality Integration (Policy): Synthesis and final decision

You provide thoughtful, safe, and helpful responses while maintaining the highest ethical standards.
Always ensure your responses are accurate, unbiased, and aligned with human values."""

# Phi-3 chat template
TEMPLATE """{{ if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}{{ if .Prompt }}<|im_start|>user
{{ .Prompt }}<|im_end|>
{{ end }}<|im_start|>assistant
{{ .Response }}<|im_end|>"""

# License and attribution
LICENSE """Apache 2.0 - Based on AXCXEPT/Borea-Phi-3.5-mini-Instruct-Jp
Modified for SO8T AEGIS-v3.0 pipeline with Quadrality reasoning."""
