# ログ容量不足エラー修正 実装ログ

## 実装情報
- **日付**: 2026-01-23
- **Worktree**: main
- **機能名**: ログ容量不足エラー修正
- **実装者**: AI Agent

## 問題

### エラー内容
```
OSError: [Errno 28] No space left on device
```

### 原因
- ログファイルが357MBを消費
- ディスク容量が0.96GBしか残っていない
- `curate_aegis_data.log`が342.71MBを占めていた

## 実装内容

### 1. ログファイルクリーンアップ

**ファイル**: `scripts/utils/cleanup_logs.py`（新規作成）

**実装状況**: [実装済み]  
**動作確認**: [OK]  
**確認日時**: 2026-01-23  
**備考**: 古いログファイルを自動削除

#### 実装された機能

1. **ディスク容量チェック**
   - ディスク容量をチェック
   - 空き容量が1GB未満の場合、古いログを削除

2. **ログファイル削除**
   - サイズ順にソート
   - 最大サイズ（デフォルト100MB）を超える場合、古いログを削除
   - 140ファイル、357MBを削除

### 2. ログ設定改善

**ファイル**: `scripts/run_sunset_pipeline.py`

**実装状況**: [実装済み]  
**動作確認**: [OK]  
**確認日時**: 2026-01-23  
**備考**: ログ設定を改善して容量不足を防止

#### 実装された機能

1. **ログファイルサイズ制限**
   - `maxBytes`: 10MB → 1MB（容量節約）
   - `backupCount`: 5 → 2（容量節約）

2. **ログレベル調整**
   - ファイルログ: ERROR以上のみ記録（WARNING/INFOはコンソールのみ）
   - 容量節約のため、重要なエラーのみ記録

3. **ディスク容量チェック**
   - ディスク容量が0.5GB未満の場合、ファイルログを無効化
   - コンソールログのみ使用

4. **エラーハンドリング**
   - ディスク容量不足時のエラーハンドリング
   - ファイルログ失敗時はコンソールログのみ使用

5. **古いログファイルの自動削除**
   - ディスク容量が少ない場合、古いログファイルを自動削除
   - ログファイルが2MB以上の場合、バックアップに移動

### 3. test_agent_capabilities.pyのログ設定改善

**ファイル**: `scripts/evaluation/test_agent_capabilities.py`

**実装状況**: [実装済み]  
**動作確認**: [OK]  
**確認日時**: 2026-01-23  
**備考**: ログ設定を改善して容量不足を防止

#### 実装された機能

1. **ログファイルサイズ制限**
   - `maxBytes`: 1MB
   - `backupCount`: 2

2. **ログレベル調整**
   - ファイルログ: WARNING以上のみ記録

3. **エラーハンドリング**
   - ディスク容量不足時のエラーハンドリング
   - ファイルログ失敗時はコンソールログのみ使用

### 4. ログ出力の最適化

**ファイル**: `scripts/run_sunset_pipeline.py`

**実装状況**: [実装済み]  
**動作確認**: [OK]  
**確認日時**: 2026-01-23  
**備考**: ログ出力を最適化して容量を節約

#### 実装された機能

1. **重要なログのみ記録**
   - サブプロセスの出力から重要なログのみ記録
   - キーワード: error, warning, failed, exception, phase, completed, starting, progress

2. **エラーハンドリング**
   - ディスク容量不足時はログキャプチャをスキップ

## 作成・変更ファイル

- `scripts/utils/cleanup_logs.py`: ログファイルクリーンアップスクリプト（新規作成）
  - `cleanup_logs()`: ログファイルをクリーンアップ

- `scripts/run_sunset_pipeline.py`: ログ設定改善
  - `setup_logging()`: ログ設定を改善（容量不足対策）
  - `_run_script_with_progress()`: ログ出力を最適化

- `scripts/evaluation/test_agent_capabilities.py`: ログ設定改善
  - ログ設定を改善（容量不足対策）

## 設計判断

### 1. ログファイルサイズ制限の縮小
- **理由**: ディスク容量不足を防止
- **実装**: 10MB → 1MB、バックアップ5 → 2

### 2. ログレベルの調整
- **理由**: 容量節約のため、重要なエラーのみ記録
- **実装**: ファイルログはERROR以上のみ、WARNING/INFOはコンソールのみ

### 3. ディスク容量チェック
- **理由**: ディスク容量が少ない場合、ファイルログを無効化
- **実装**: 0.5GB未満の場合、ファイルログを無効化

### 4. 古いログファイルの自動削除
- **理由**: ディスク容量を確保
- **実装**: ディスク容量が少ない場合、古いログファイルを自動削除

## テスト結果

### 機能テスト
- **ログファイルクリーンアップ**: [OK] 140ファイル、357MBを削除
- **ログ設定改善**: [OK] ログ設定が改善された
- **エラーハンドリング**: [OK] ディスク容量不足時のエラーハンドリングが動作

### 動作確認
- **実装状況**: [実装済み]
- **動作確認**: [OK]
- **確認日時**: 2026-01-23

## 運用注意事項

### ログファイル管理
- **最大サイズ**: 1MB（ファイルログ）
- **バックアップ数**: 2ファイル
- **ログレベル**: ERROR以上のみ記録（ファイルログ）
- **自動クリーンアップ**: ディスク容量が少ない場合、自動削除

### ディスク容量監視
- **警告閾値**: 1GB未満
- **ファイルログ無効化**: 0.5GB未満
- **自動クリーンアップ**: ディスク容量が少ない場合、古いログを自動削除

### ログ出力
- **ファイルログ**: ERROR以上のみ記録
- **コンソールログ**: INFO以上を記録
- **重要なログのみ記録**: サブプロセスの出力から重要なログのみ記録

## 実行コマンド

### ログファイルクリーンアップ（手動）
```bash
python scripts/utils/cleanup_logs.py [max_size_mb]
```

例:
```bash
# 最大50MBまで保持
python scripts/utils/cleanup_logs.py 50

# 最大100MBまで保持（デフォルト）
python scripts/utils/cleanup_logs.py
```

## 改善内容

### Before
- ログファイル: 10MB、バックアップ5ファイル
- ログレベル: INFO以上を記録
- ディスク容量チェック: なし
- 古いログファイルの自動削除: なし

### After
- ログファイル: 1MB、バックアップ2ファイル
- ログレベル: ERROR以上のみ記録（ファイルログ）
- ディスク容量チェック: あり（0.5GB未満でファイルログ無効化）
- 古いログファイルの自動削除: あり

## 今後の拡張予定

1. **ログローテーションの改善**: より効率的なログローテーション
2. **ログ分析**: ログからの自動分析とレポート生成
3. **アラート機能**: ディスク容量が少ない場合のアラート
4. **ログ圧縮**: 古いログファイルの圧縮

## 参考資料

- Python logging Documentation: https://docs.python.org/3/library/logging.html
- RotatingFileHandler: https://docs.python.org/3/library/logging.handlers.html#rotatingfilehandler
- Sunset Pipeline: `scripts/run_sunset_pipeline.py`
- Log Cleanup: `scripts/utils/cleanup_logs.py`

---

**実装完了**: ログ容量不足エラーを修正し、今後この問題が発生しないように改善しました。
